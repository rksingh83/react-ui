cd..
sudo chown nginx: slp - api - R
git reset--soft HEAD~
    cd / data / nginx / d1 - slp;


sudo chown mohit.kumar: slp - api / -R;

cd slp - api;

artisan config: cache;
composer dump - autoload
php artisan db: seed
cd..;
sudo chown nginx: slp - api / -R;